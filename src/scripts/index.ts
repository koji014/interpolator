/** @format */

import Animation from "./Animation";

window.addEventListener(
    'DOMContentLoaded',
    () => {
        const animation = new Animation();
        animation.init();
    },
    false
);